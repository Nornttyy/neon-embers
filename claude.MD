# 《霓虹余烬》俯视角动作游戏（单机 + 多人）开发规范

## 1. 项目定位

本项目是一款低精度赛博科技画风的 2D 俯视角动作网页游戏，不采用肉鸽玩法。

核心不是自动攻击，而是玩家主动操作：近战连击、少量远程武器、闪避、格挡和精准招架。游戏同时支持完整单机体验，以及真正联网的多人闯关和多人对战。

当前公开版本是三关固定流程的单机动作闯关，用于验证手动战斗、美术和发布流程。不能加入局内随机三选一、随机强化构筑或把本地机器人包装成真人联机。

## 2. 核心原则

- 先把打击感和动作系统做好，再扩充地图与装备数量。
- 单机必须可以完整游玩，不得只是多人模式的练习场。
- 多人闯关支持 2～4 名真实玩家；对战首版支持 1v1 和 2v2。
- 玩法信息清楚，但减少不必要的弹窗、红点和长篇提示。
- 画面可以简洁，动作反馈、碰撞判定和网络同步不能偷工减料。
- 广告只能是可选增益或结算奖励，不能直接出售对战属性。
- 多人对战明确分为“统一起点的平衡玩法”和“自带装备的高风险玩法”，两者分开匹配、分开奖励和统计。

## 3. 游戏模式

### 3.1 单机模式

单机可以离线运行，包含两种入口：

1. 任务闯关：在任务外选择角色和固定装备，按设计好的敌群、精英与首领顺序完成关卡，不在战斗中随机强化。
2. 机器人试练：选择敌人、武器和难度，用于练习连招、闪避、格挡与招架。

单局目标时长为 10～15 分钟。单机拥有独立且完整的成长、解锁和存档，不要求登录。

### 3.2 多人闯关

- 支持 2～4 人快速匹配和私人房间邀请。
- 玩家共同通过固定任务房间、精英战和首领战。
- 队友倒地后可以救援，救援有明确风险和打断条件。
- 敌人的生命、攻击欲望和组合随队伍人数调整，不能只粗暴增加血量。
- 默认关闭友军伤害。
- 任务掉落以个人结算资源为主，不在任务中随机掉落属性装备，避免抢装备引发冲突。
- 玩家掉线后保留短暂重连窗口；无法重连时再由明确标注的机器人临时接管。

### 3.3 多人对战

多人对战共用 1v1、2v2、快速匹配和私人房间功能，但分成两种规则完全独立的玩法。

#### 武器升级战

- 所有玩家以相同等级的基础武器和属性进入比赛。
- 击败敌人、助攻或完成地图目标可获得本局武器能源。
- 武器能源只能沿比赛开始前公开的固定分支升级武器，分支和数值不随机。
- 每局结束后清空临时升级，局外已购买装备不能带入。
- 该玩法强调操作、路线选择和公平竞技，结算奖励为中等。

#### 装备歼灭战

- 玩家可以把自己此前使用核心能源购买并解锁的武器、技能和装备带入比赛。
- 每回合被击败后不能立即复活，一方全部被消灭即输掉该回合。
- 采用多回合胜负制，可加入装备损耗、带出奖励或入场成本，形成高风险高收益循环。
- 结算奖励比武器升级战丰厚，但因为玩家装备不同，平衡性较差。
- 匹配时参考队伍人数、装备评分和近期表现，减少新玩家直接遇到顶级装备的情况，但不承诺完全公平。
- 进入匹配前必须清楚显示该模式允许自带装备，不能把它包装成公平排位。

广告不能在任何对战模式中临时增加伤害、生命或体力。后续确认战斗稳定后，再考虑扩展 6v6 或更大的战地玩法。

## 4. 基础操作

### 4.1 键鼠

- `WASD` 或方向键：移动。
- 鼠标：瞄准和调整角色朝向。
- `1`：选择球体核心近战槽；`2`：选择轨道手枪槽，新任务默认槽 1。纵向鼠标滚轮累计到阈值后也可切换，同一滚动手势只允许切换一次；横向滚动和缩放手势不能误触发。
- 鼠标左键：使用当前武器槽；选择手枪后朝鼠标方向射击，近战槽连续输入形成三段连击。
- `J`：当前武器的键盘辅助操作，不作为主要射击方式。
- 鼠标右键或 `E`：按住格挡，在正确时机按下触发精准招架。键盘、鼠标、触屏分别记录防御来源，所有来源都释放后才能结束防御；`K` 不绑定动作。
- `Space`：朝移动方向闪避。
- `R`：只在已经装备的非空技能槽之间循环；`Q`：释放当前技能。当前核心技能默认位于槽 1，`3` 不绑定动作。

### 4.2 触屏

- 左侧虚拟摇杆控制移动。
- 右侧方向区控制朝向。
- 独立提供攻击、格挡、闪避、远程和技能按钮；“攻击”使用当前武器槽，“远程”快捷射击且不改变当前槽，“技能”释放当前高亮技能。
- 重要按钮不能挤在屏幕边缘；攻击与闪避应方便同时操作。

键位和触控布局应允许玩家在设置中调整。

## 5. 动作战斗系统

### 5.1 角色状态

角色至少包含以下明确状态：

- 待机、移动、冲刺。
- 轻攻击一至三段、重攻击或武器特殊动作。
- 远程攻击。
- 格挡、精准招架、破防。
- 闪避及短暂无敌帧。
- 受击、硬直、击退、倒地和死亡。

状态切换必须由统一状态机控制，不能通过零散计时器互相覆盖。

### 5.2 近战

- 普通轻攻击使用三段连击，每段拥有独立的前摇、有效帧和后摇。
- 不同武器拥有不同攻击范围、角度、位移、硬直和取消窗口。
- 攻击判定使用独立 hitbox，受击判定使用 hurtbox。
- 同一次攻击不能对同一目标重复结算伤害，除非该动作明确设计为多段攻击。
- 命中后提供短暂停顿、闪光、音效、粒子和轻微镜头震动。
- 高冲击武器可削减敌人的韧性并造成硬直或击退。

### 5.3 体力

- 闪避、持续格挡和重型动作消耗体力。
- 停止消耗一小段时间后，体力自动恢复。
- 体力耗尽时不能闪避，格挡容易被破防。
- 体力条必须常驻且易读，不能只靠颜色或音效表达。

### 5.4 闪避

- 闪避拥有短暂无敌帧，但不能覆盖整个动作。
- 闪避距离、无敌时间、体力消耗和恢复时间都由配置控制。
- 闪避可取消部分攻击后摇，但不能无限取消所有动作。
- 连续闪避应受到体力和恢复时间限制。

### 5.5 格挡与招架

- 格挡只防御角色正面的攻击，背后攻击仍然命中。
- 普通格挡会减少伤害并消耗体力，部分强攻击会造成少量穿透伤害。
- 敌人攻击命中前约 120～180 毫秒内开始格挡，可触发精准招架；最终窗口以测试结果为准。
- 精准招架不受伤或只受极少伤害，并使攻击者短暂失衡。
- 体力被打空后触发破防，角色进入明显硬直。
- 无法格挡的攻击必须使用统一的危险颜色和声音提前提示。

### 5.6 远程攻击

- 远程是战术补充，不替代近战核心。
- 远程武器使用弹药、热量或冷却机制限制持续输出。
- 投射物需要清楚的轨迹和命中反馈。
- 高伤害远程攻击必须有明显蓄力、后摇或资源成本。

## 6. 武器规划

首版垂直切片先完成三把差异明显的武器：

1. 能量剑：速度和伤害均衡，适合学习三段连击与招架。
2. 动力锤：攻击缓慢、削韧高，可蓄力破防。
3. 轨道手枪：精准远程攻击，弹匣有限，换弹期间容易被近身。

后续扩展：

- 双刃：攻击快、位移多、格挡能力弱。
- 长枪：攻击距离长，正面控制强，侧面较弱。
- 盾刃：防御和反击强，普通输出较低。
- 霰弹枪：近距离爆发和击退，换弹慢。
- 能量弓：蓄力越久伤害越高，移动时难以保持精度。
- 投掷刃：可以回收或召回，适合制造往返伤害。
- 榴弹发射器：范围控制强，但弹药稀少且存在安全距离。

每种武器必须有自己的攻击动画、范围、节奏、资源消耗和风险，不能只替换数值与颜色。

### 6.1 技能规划

技能分成武器技能和角色技能。运行时保留三个可扩展技能槽和各自独立冷却，`R` 只循环非空槽、`Q` 释放当前槽；HUD 任意时刻只能高亮一个已装备技能。当前垂直切片每个球体只在槽 1 装配一个核心技能，槽 2、3 明确显示为待装配，空槽不得被选中或误触发。未来增加武器技能或第二角色技能时复用同一套槽位，不增加临时快捷键。

武器技能随当前武器变化：

- 能量剑——弧光反击：成功招架后释放一道短距离剑气。
- 动力锤——震荡重砸：蓄力攻击地面，破坏范围内敌人的韧性与格挡。
- 双刃——相位追斩：快速穿过目标并从背后追加一次斩击。
- 长枪——磁轨突刺：向瞄准方向突进，命中第一个目标后停止。
- 盾刃——屏障推进：举盾前进，抵挡正面投射物并推开敌人。
- 轨道手枪——过载射击：消耗剩余弹匣打出一发高穿透攻击。
- 霰弹枪——反冲跃射：向前开火，同时借后坐力向后撤离。
- 能量弓——锁定箭雨：标记一块区域，短暂延迟后落下箭雨。

角色技能提供流派选择：

- 能量护盾：短时间吸收伤害，但不能替代主动格挡。
- 瞬步：快速位移并在原地留下短暂诱饵。
- 脉冲陷阱：减速进入范围的敌人。
- 战术治疗：持续施放后恢复自己或队友，受击会被打断。
- 电磁干扰：暂时禁用敌人的远程能力或机械装置。
- 战吼：短时间提高自己和附近队友的韧性恢复。

技能拥有冷却、充能次数或施放风险，不能无条件连续释放。这里的“充能”是战斗次数限制。武器升级战使用统一技能池；装备歼灭战允许携带已解锁技能。

## 7. 固定装备与任务奖励

- 单机和多人闯关都在任务开始前确定角色、武器和技能，进入任务后不出现随机三选一、随机属性或临时构筑。关卡之间允许回到固定整备房间购买即时补给，但商品与效果不能随机。
- 任务中的武器伤害、范围、速度和动作由固定配置决定；玩家依靠操作、路线和团队配合完成目标。
- 关卡可提供固定位置的治疗、弹药和任务装置，但效果必须预先设计并保持一致。
- 击败敌人获得回收能源。能源可以在关卡之间的整备房间购买修复、弹药、体力和临时护盾，也参与任务记录和结算；战斗进行中不能购买物品，任何商品都不能随机强化属性。
- 核心能源是任务结算后的局外资源，用于有上限的装备校准、武器解锁、技能解锁和外观，不无限堆叠基础属性。
- 单机和多人合作可以读取已装备的固定校准；武器升级战忽略永久数值并使用统一起点；装备歼灭战读取已解锁装备。
- 广告只能用于可选的单机复活、结算奖励或外观试用，不能触发随机属性，也不能在多人对战中提供数值优势。

## 8. 敌人与关卡

- 敌人攻击前必须有可辨认的动作、方向和声音提示。
- 小怪分别承担近战压迫、远程骚扰、区域封锁和支援职责。
- 同屏组合应制造决策，不以大量高血量敌人拖延时间。
- 房间由手工规则和配置组合生成，保证入口、出口和安全区域可用。
- 每个首领至少包含两个阶段，并检查玩家是否理解闪避、格挡和位置控制。
- 联机时敌人的目标选择和技能释放由服务器决定。

## 9. 新手教程与界面

首次进入游戏时，通过一段可操作训练依次学习：

1. 移动和瞄准。
2. 使用数字键 `1`、`2` 或纵向鼠标滚轮切换近战和轨道手枪槽。
3. 使用当前槽完成三段轻攻击和一次远程射击。
4. 按住 `E` 或鼠标右键格挡，并完成一次精准招架。
5. 闪避指定攻击。
6. 使用 `R` 查看当前已装备技能，再按 `Q` 释放；当前只有一个技能时 `R` 保持原槽。
7. 切换到武器槽 2，并使用鼠标左键命中一次远程目标。
8. 完成一间实战房。

教程应在玩家完成动作后自动继续，不使用长篇说明。玩家可以跳过，也可以从训练入口重新开始。

主菜单只保留清晰入口：单机、多人闯关、多人对战、训练、装备和设置。进入多人对战后，再选择“武器升级战”或“装备歼灭战”，并直接显示公平程度和奖励等级。尚未接入服务器的模式必须标为“开发中”，不能显示虚假的在线人数或匹配结果。

## 10. 联机技术要求

- 使用服务器权威架构：伤害、生命、体力、攻击状态、掉落和胜负由服务器最终裁定。
- 客户端负责输入采样、本地预测、画面表现和声音，不能自行声明命中结果。
- 其他玩家使用插值显示；必要时对近战使用有上限的延迟补偿。
- 所有攻击携带动作编号、开始时间和方向，服务器拒绝非法连招、超速移动和异常攻击频率。
- 对格挡、招架和闪避分别制定延迟容忍规则，保证公平且不产生明显吞输入。
- 开发阶段必须模拟 50、100、150 和 250 毫秒延迟，以及丢包和重连情况。
- GitHub Pages 只能托管静态客户端，真正联机还需要独立的游戏服务器、房间服务和匹配服务。
- 网络状态差时显示简洁图标，并允许安全退出或重连，不连续弹窗打断战斗。

## 11. 广告与商业化边界

可以在后续接入激励广告，但首版先保留可替换接口。

允许的场景：

- 单机失败后获得一次复活。
- 单机或合作结算时增加局外货币。
- 免费刷新一次局内奖励选择。
- 获取纯外观试用。

禁止的场景：

- 战斗中强制插播。
- 观看广告增加 PvP 伤害、生命、体力或匹配优势。
- 利用误触关闭按钮或连续弹窗强迫观看。
- 合作队伍中一名玩家看广告时强制其他玩家等待。

## 12. 代码结构目标

现有原型应逐步拆分为以下职责：

- `engine/`：游戏循环、时间、输入、碰撞、摄像机和对象生命周期。
- `combat/`：动作状态机、武器、hitbox、hurtbox、伤害、体力、格挡和闪避。
- `entities/`：玩家、敌人、投射物和首领。
- `modes/`：训练、单机闯关、合作闯关和竞技对战。
- `network/`：连接、房间、快照、预测、插值和重连。
- `ui/`：菜单、HUD、教程、设置和结算。
- `content/`：武器、技能、敌人、固定任务、装备校准和奖励配置数据。
- `services/`：存档、账号、能源结算、音频、统计和广告接口。

动作数据应尽量配置化，避免每增加一把武器都修改主循环。

## 13. 分阶段制作流程

### 阶段 1：本地动作垂直切片

完成一个训练房、一名玩家、一种近战武器、一种远程武器、一个武器技能、一个角色技能和两种敌人。必须具备移动、三段攻击、闪避、格挡、招架、体力、技能、受击和死亡。

验收标准：连续游玩 10 分钟无严重错误；键鼠和触屏都可完成教程；打击与受击反馈清楚。

### 阶段 2：单机闯关

加入固定房间流程、任务目标、回收能源、核心能源、三种近战武器、两种远程武器、六种敌人、一名首领、结算和本地存档。

验收标准：一局拥有明确开局、固定阶段、精英、首领与任务结算；流程可重复练习且不会随机改变玩家属性；离线刷新页面后仍能保存任务记录和已解锁装备。

### 阶段 3：网络垂直切片

搭建权威服务器，先让两个客户端在测试房中移动、攻击、格挡、受伤、死亡和重连。

验收标准：在 150 毫秒模拟延迟下仍能正常对战；服务器可以拒绝明显非法输入；不能通过修改客户端直接改血量或伤害。

### 阶段 4：多人闯关

完成 2～4 人房间、快速匹配、私人邀请、队友倒地救援、人数难度缩放、个人奖励和合作首领。

验收标准：四名真实客户端可以完成一局；一人掉线后其余玩家仍能继续并允许重连。

### 阶段 5：多人对战

先完成 1v1 和 2v2 武器升级战，包括统一起始装备、局内武器分支、中等奖励、匹配、私人房和赛后再来一局；稳定后再完成允许携带已购装备、多回合无复活和丰厚奖励的装备歼灭战。

验收标准：武器升级战中不同账号的局外成长不影响竞技属性；装备歼灭战能正确读取和校验玩家已购装备，并按装备评分辅助匹配；两种模式的奖励与统计完全分开；高延迟与断线有明确处理；胜负和奖励都由服务器裁定。

### 阶段 6：内容、优化与商业化

扩展武器、角色、地图和首领，优化手机性能与加载速度，再接入可关闭的激励广告和基础数据统计。

验收标准：广告关闭或加载失败不影响游戏；手机端稳定运行；隐私说明和广告用途清楚。

## 14. 第一版交付范围

下一版先只实现阶段 1，不直接同时开发完整多人系统。第一版应让玩家真正感受到：

- 近战三段连击有节奏和打击感。
- 闪避可以躲开危险，但不能无限使用。
- 格挡和精准招架有明确区别。
- 远程武器能解决特定情况，但无法取代近战。
- 武器技能和角色技能拥有明确用途、冷却与反馈。
- 教程短而有效。
- 页面可以直接打开和游玩，不出现虚假的联网功能。

完成阶段 1 并确认动作手感后，再开始单机闯关和服务器联机，避免在核心战斗尚未成立时过早扩展内容。

## 15. 当前动作闯关（0.9.4）

- 公开仓库以源码为准，禁止手工提交 `dist/` 充当源码。任何推送到 `main` 的提交必须由 `.github/workflows/deploy-pages.yml` 先运行检查和构建，成功后再自动部署 GitHub Pages；测试失败不得覆盖当前线上版本。

当前网站已经从自动攻击原型改为手动动作战斗切片，已完成：

- 玩家外观改为可实时表现朝向和状态的科技小球。
- 三种可选近战模块：弧光剑、相位双刃和动力锤，各有独立三段攻击节奏。
- 轨道手枪远程副武器，包含弹匣、射击间隔和自动装填。
- HUD 固定显示编号 1 的核心近战槽和编号 2 的轨道手枪槽，并且任何时刻只能有一个明确高亮。新任务默认武器槽 1；数字键 `1`、`2` 选择武器，纵向滚轮累计 48 像素后切换，并使用 160 毫秒手势窗口保证一次滚动只切一次。横向滚动、缩放手势和未达阈值的细小滚动不得误切；切换本身不得攻击、扣除弹药或改写球体的核心近战配置。
- 鼠标左键使用当前武器槽，槽 2 必须朝鼠标方向射击；`J` 只保留为同一主攻击的键盘辅助入口。触屏“攻击”使用当前槽，触屏“远程”保留独立射击入口。`attack`、`block`、`dash`、`broken`、`skill` 状态收到的武器切换请求必须排队，并在玩家回到 `idle` 后应用，禁止在挥砍或其他完整动作中途替换显示武器与碰撞判定。
- 技能系统保留三个可扩展槽、三个独立冷却与唯一当前槽。当前每个球体只在槽 1 装配自身核心技能，槽 2、3 显示为待装配；`R` 循环时只访问非空技能，当前只有一个技能时保持原槽，未来注入多个技能后按槽位循环；`Q` 和触屏“技能”释放当前高亮技能。按键重复、空槽、`3` 和 `K` 均不得造成额外动作。
- 按住 `E`、鼠标右键或触屏格挡进入防御；三类输入按来源聚合，释放其中一个来源时，只要另一来源仍按住就必须继续防御，全部释放后才返回空闲。
- 体力、闪避无敌帧、正面格挡、精准招架与破防。
- 三种球体主动技能：脉冲斩、超频和应急护盾。
- 敌人近战前摇、远程弹幕、精英敌人与训练首领。
- 当前单机任务由 `1-1 外围突破`、`1-2 火力封锁`和 `1-B 核心决战`三个固定关卡组成。清除本关全部目标后才能进入下一战区，不使用计时触发首领，也不允许无限生成敌人。
- 每关使用明确、可测试的固定敌人序列与同屏上限；第二关加入远程、护盾、重装、突进与精英组合，第三关只进行首领决战。
- 选择球体后以及每次清关后都会进入同一个整备房间。玩家可查看下一关目标、剩余生命、体力、弹药和护盾，购买固定补给后手动开始战斗。
- 房间状态与战斗状态必须分离：清关后只更新“下一战”目标，不得提前切换当前关卡、加载敌人队列或使用下一战区背景；只有玩家点击开始战斗后才正式激活下一关。
- 首次进入整备房间获得 25 点启动能源；敌人掉落的回收能源可购买装甲修复包、轨道弹匣、动能电池和应急护盾。商品列表、价格和效果全部固定，不随机刷新。
- 清关时自动回收地面能源并清除残留弹幕，但不会免费恢复生命；玩家能源不足或不想购买时可以直接开始下一关。
- 已删除战斗内随机三选一、随机能源终端、刷新按钮、临时等级和随机强化配置，战斗不会因构筑选择暂停。
- 键鼠与触屏操作入口。
- 科技小球实际持有武器，三段近战通过武器图片围绕握点旋转完成，不使用扇形色块代替挥砍。
- 相位双刃使用单独的刀刃素材绘制两次，拥有两个握点；一、二段交替挥刀，第三段反向交叉斩击。
- 每段挥砍必须分为可读的蓄力、快速命中和回收阶段；玩家在动作期间输入攻击时缓冲下一段连击，不能因按键略早而吞输入。
- 近战伤害使用与画面武器姿态一致的刀刃胶囊或锤头碰撞体，只有怪物实际接触武器才结算；禁止用角色前方扇形冒充武器碰撞。
- 武器的显示尺寸与碰撞长度集中配置。不同素材先统一视觉尺度，再按刀刃或锤头的有效部位校准判定；攻击段数与强化不得缩放武器，范围成长只能扩大挥击角度或改变位移。
- 删除覆盖角色的大月牙挥砍图片，但保留并恢复程序生成的动态刀光。玩家攻击的有效阶段必须每帧从当前武器碰撞形状取刀刃端点，用带生命周期的连续采样点绘制渐隐轨迹；双刃必须有两组独立采样，不得把两把武器的刀光连成一条。
- 动态刀光只能使用 Canvas 路径等实时绘制，不得读取 `slash-arc.png`、`slashSprite`、`vfxSlash` 或其他静态挥砍贴图；不得额外绘制半透明的武器图片当作残影。攻击中每把真实武器精灵只绘制一次，挥空不得向贴图特效队列添加新项；武器实际接触敌人后仍显示对应命中反馈。
- 动态刀光的战斗绘制路径中禁止使用 `shadowBlur`、`filter` 或 `drawImage`。刀刃用 17 像素透明外层与 3 像素高亮内层，动力锤用 24/4 像素两层，两层均为程序折线并使用 `lighter` 混合；不得为了性能减小这些视觉宽度。
- 每段攻击使用 8～10 个关键姿态表示蓄力、有效帧和回收，并按动作时间动态插值握点与角度。显示姿态和武器碰撞必须读取同一份插值结果，避免武器已经碰到敌人但判定仍停在上一帧。
- 防御、格挡命中、精准招架、破防、技能、弹丸、枪口、不同武器命中、敌人攻击/突进/生成/护盾/销毁、能源拾取、闪避和首领爆发全部使用统一画风的透明 PNG 素材。每类反馈使用 8～10 个关键帧参数动态插值位置、旋转、缩放和透明度，不使用视频文件代替动作状态。
- 按住格挡时必须持续显示面向攻击方向的防御场；普通格挡、精准招架、体力耗尽破防和临时护盾受击各自使用不同素材与时间曲线，不能只在挡到攻击后临时画一条圆弧。
- 音效使用分层程序音频，区分挥空和实际命中，并为剑刃、双刃、重锤、射击、格挡、精准招架、装填与技能提供不同材质反馈。命中音至少包含瞬态脆响、低频冲击和材质共鸣。
- 战斗中播放轻量程序化赛博音乐，包含鼓点、低音和合成琶音；音乐拥有独立音量设置，暂停时自动压低，退出或结算时停止。
- 高频战斗事件最多保留一层真实采样和一层必要的低频或能量音色，不得为追求打击感无限叠加噪声。真实采样必须记录来源与许可，并控制总下载体积。
- 敌人受击默认使用钝冲击而不是刀刃互撞声；高频金属采样仅用于格挡、招架和机械装填。闪避必须表现为空气快速掠过与推进音，禁止使用类似爆炸或受击的低沉下坠声。
- 当前近战挥空统一使用无金属尾音的短促空气呼啸，按单剑、双刃和重锤调整播放速度与截断长度；敌人受击使用手枪弹丸撞击采样，重击差异通过低频层表达，不能换回玻璃或刀刃碰撞声。
- 程序音频噪声缓存复用；Canvas 像素倍率设上限，并减少普通敌人的高成本阴影。镜头震动使用连续波形，避免逐帧随机抖动造成视觉卡顿。
- 敌人受击高亮以及玩家闪避/技能状态禁止在战斗渲染时设置 `ctx.filter`。所有需要亮度、饱和度或色彩变化的角色图必须在加载阶段以原始尺寸预生成等视觉缓存，运行时只通过 `drawImage` 切换原图或缓存图。缓存创建阶段必须出现在加载进度中，并在主画布 VFX 预热前完成；不得降低原精灵尺寸或跳过状态反馈。
- 地板、全部场景物件和场地边界必须由 `drawArenaStatic` 完整绘制到当前 DPR 的单一场景 Canvas，`renderArena` 在缓存就绪时每帧只调用一次 `drawImage`。缓存四周必须增加 160 像素透明边距以保留边界 PNG 和阴影外溢；键包含房间/关卡、关卡索引和 DPR。进入房间、开始战斗与窗口 DPR 变化都必须重建；新缓存提交主画布并等待三帧后才允许隐藏加载层，交换后旧 Canvas 的宽高置 1 释放显存。缓存不可用时必须回退到完整 `drawArenaStatic`，不得删除场景素材。
- 性能根因必须以实测为准。Chrome 151 / Apple M5 Pro Metal / 120 Hz / 1280×800 下，旧实现每帧重绘约 80 个带阴影静态物件，基线 p50/p95 8.3/8.4 ms 已吃满帧时余量；受击 16.7 ms、闪避 18.1 ms、`screen` 16.6 ms、阴影刀光 33.3/25.1 ms。仅禁用每帧场景重绘即恢复 8.3～8.4 ms，因此真正根因是静态场景成本，实时滤镜与刀光阴影是放大项。修复后刀光/受击/闪避/`screen` 均回到 8.3～8.4 ms 附近；击杀后 49 帧 p95/最大值 8.4/8.4 ms，超过 12 ms 的帧为 0。不得删减特效数量来伪造性能改善。
- 玩家进入整备房前，除了等待全部战斗图片完成加载与解码，还必须把所有特效以互不重叠的网格同时预绘到真实游戏 Canvas；不能用 detached Canvas，也不能让多张图在同一次提交前互相覆盖，因为这两种做法都不会替主画布完成全部纹理上传。`source-over`、`lighter` 和 `screen` 三种混合管线分别提交并各等待至少三个渲染帧，预热失败只记录并使用原图片或几何兜底，不阻断游戏流程。
- 特效预热覆盖 `source-over`、`lighter` 和 `screen` 三种游戏实际使用的混合管线，每次网格提交后都至少等待三个渲染帧。优化卡顿不得降低素材分辨率、关键帧数或特效数量。
- 页面必须从初始 HTML 起显示可拦截输入的轻量加载层，不使用视频、大图或高成本模糊背景。进度条使用已完成的图片加载/解码数量和失败数量计算，并显示角色缓存与特效普通/发光/屏幕混合阶段；初始层只在包含 VFX 预热和预滤镜角色缓存的 `game.assetsReady` 完成后隐藏。玩家选择球体后，任务加载层必须继续等待 `audio.loadSamples()` 和目标 `arenaReady`，再进入整备房或战斗。各流程结束时必须显示 100%，同时更新 `aria-valuenow`、`aria-busy` 和可见性；后台离线安装不得阻塞初始界面或开战。
- 关卡内按固定队列逐个投入敌人，同屏上限由关卡配置控制（当前普通关为 4、首领关为 1），避免小场地持续堆满敌人；全局安全上限为 32。
- 图片素材统一使用石墨黑/深海军蓝装甲、青色边光和高饱和能源核心，保持俯视三分之四视角、清楚轮廓、少量大结构、低至中等细节；禁止一张图写实、一张图卡通或靠改色冒充所有职能。
- 使用图片生成制作剑锋、相位、壁垒三种玩家球体，弧光剑、两把分离的相位双刃、动力锤、轨道手枪、能源核心和四种整备商品；近战、疾行、远程、重装、护盾、突进、精英、Boss 八类敌人必须拥有独立机体素材，不能再靠基础敌人改色冒充不同职能。
- 场景素材覆盖整备房、外围、封锁区和核心区地面，以及能源终端、电力柱、场地屏障和通风装置。场地网格可以程序化滚动，但不能继续用纯色矩形充当所有装饰与功能物件。
- 普通与重型敌人的攻击预警分别使用定向图片并按 8～10 个关键帧动态推进，不能残留可见的程序扇形；初始校准、备用装甲和回收协议三项永久升级卡也必须通过 `assetUrl` 显示对应素材。
- 玩家机体按核心类型切换并随瞄准方向旋转，选人卡片必须显示对应玩家球体。玩家身体和武器保持为独立图片层，禁止把武器烘焙进身体素材，以免破坏握点动画和真实武器碰撞。
- 敌人素材按职能接入实际 Canvas 渲染并随朝向旋转；攻击预警、挥击、弹丸、枪口、突进拖尾、护盾、生成和销毁反馈使用对应图片层，受击高亮、数值文字和血条可由程序层叠加。图片未加载时才允许使用简化几何体兜底。
- 所有游戏内 PNG 必须带真实透明通道并在接入前检查，不能把棋盘格、白底或黑底烘焙进素材。原始生成图保留在生成目录，网站使用等比缩放后的轻量副本。
- 玩家进入整备房间前必须等待战斗图片完成加载与解码；素材请求失败时使用带版本参数的新请求重试，不能让短时防御、闪避或命中特效在图片未就绪时直接错过。
- 生产构建必须根据公开文件内容自动生成统一资源版本号，并将版本写入 HTML、ES 模块、图片、音频和 Service Worker 缓存键。替换同名素材后不得继续命中旧 URL。
- Service Worker 只能读取当前版本的游戏缓存，清理时只能删除 `neon-embers-` 前缀的旧缓存。新版 Worker 接管已有页面时只执行一次带缓存穿透参数的刷新，首次安装不得重复刷新。
- Service Worker 必须保留全部 86 项、当前约 10,554,979 字节的公开离线资源，使用固定 4 个 worker 的有界队列，禁止对全部素材使用无界 `Promise.allSettled`。注册必须同时等待窗口 `load` 事件和包含 52 张运行时图片加载、解码、角色缓存及 VFX 预热的 `game.assetsReady`，两者都完成后才能开始，避免冷启动与离线安装争抢带宽；离线安装在后台进行，绝不能成为初始加载或开始任务的等待条件。核心页面和模块必须全部成功才安装该版；可选图片/音频失败只记录并继续后续队列。每项完成后通过 `CACHE_PROGRESS` 发送 `loaded/total/failed/ready`，带内容版本的 URL 复用 HTTP 缓存。

当前多人入口仍未开放。这一版只用于验证固定流程的单机动作闯关；真正多人必须在后续权威服务器阶段实现。
